import { CoordinatorPage } from "@/components/coordinator/coordinator-page";

export default function CoordinatorRoutePage() {
  return (
    <main className="coordinator-route">
      <CoordinatorPage />
    </main>
  );
}
